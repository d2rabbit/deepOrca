/**
 * Intentionally empty module (see ../package.json): every unmounted SDK
 * parser package resolves here so dead dynamic-import chunks disappear from
 * the renderer bundle (specs/artifact-landing A6). These paths never execute
 * in supported preview flows.
 */
export {};
