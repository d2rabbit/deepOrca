(function(){'use strict';var root=document.documentElement,reduce=window.matchMedia('(prefers-reduced-motion: reduce)').matches,theme=document.getElementById('theme');function setTheme(t){root.dataset.theme=t;try{localStorage.setItem('deeporca-theme',t)}catch(e){}}setTheme(root.dataset.theme==='light'?'light':'deep');theme.addEventListener('click',function(){setTheme(root.dataset.theme==='deep'?'light':'deep')});var scenes=[].slice.call(document.querySelectorAll('.scene')),stops=[].slice.call(document.querySelectorAll('.depth-stops button')),readout=document.getElementById('depth-readout'),depths=['000m|surface','380m|shape','760m|make','1100m|connect','1400m|logbook'],current=0,locked=false;function goTo(i){if(i<0||i>=scenes.length)return;current=i;scenes[i].scrollIntoView({behavior:reduce?'auto':'smooth'});stops.forEach(function(s,n){s.classList.toggle('active',n===i)});var p=depths[i].split('|');readout.innerHTML=p[0]+'<br><small>'+p[1]+'</small>'}stops.forEach(function(s){s.addEventListener('click',function(){goTo(Number(s.dataset.go))})});document.getElementById('enter-dive').addEventListener('click',function(){goTo(1)});if(!reduce){var io=new IntersectionObserver(function(entries){entries.forEach(function(e){if(e.isIntersecting){var i=scenes.indexOf(e.target);if(i>-1&&!locked){current=i;stops.forEach(function(s,n){s.classList.toggle('active',n===i)});var p=depths[i].split('|');readout.innerHTML=p[0]+'<br><small>'+p[1]+'</small>'}e.target.classList.add('seen')}})},{threshold:.55});scenes.forEach(function(s){io.observe(s)})}else scenes.forEach(function(s){s.classList.add('seen')});window.addEventListener('keydown',function(e){if(['ArrowDown','PageDown'].indexOf(e.key)>-1){e.preventDefault();goTo(Math.min(current+1,scenes.length-1))}if(['ArrowUp','PageUp'].indexOf(e.key)>-1){e.preventDefault();goTo(Math.max(current-1,0))}});
var phaseData=[['01 / INTENT','一句需求，先被真正听见','用自然语言描述你的流程，DeepOrca 从意图开始理解，而不是从一堆选项开始。'],['02 / PROTOTYPE','流程开始呼吸','低保真原型快速成形，关键动作被看见、被点击、被验证。'],['03 / DESIGN','视觉长出秩序','设计系统、色彩和层级沿着意图展开，成为可以独立交付的设计语言。'],['04 / CODE','想法真正运行','代码、变更与上下文连进同一个 Agent 循环，让结果不止停在画布上。']];var phaseButtons=[].slice.call(document.querySelectorAll('[data-phase]')),parts=[].slice.call(document.querySelectorAll('.phase-part')),phaseNo=document.getElementById('phase-no'),phaseTitle=document.getElementById('phase-title'),phaseText=document.getElementById('phase-text');function renderPhase(i){phaseButtons.forEach(function(b,n){b.classList.toggle('active',n===i)});parts.forEach(function(p,n){p.classList.toggle('show',n===i)});phaseNo.textContent=phaseData[i][0];phaseTitle.textContent=phaseData[i][1];phaseText.textContent=phaseData[i][2]}phaseButtons.forEach(function(b){b.addEventListener('click',function(){renderPhase(Number(b.dataset.phase))})});renderPhase(0);
var podData={chat:['01 / WORKBENCH','AI 会话工作台','流式会话、工具调用与上下文压缩在同一循环中发生，让任务从一个念头持续长成可接续的工作。'],editor:['02 / WORKBENCH','专业编辑器','把思考直接送进文件；内联差异让每一次 AI 变更都能被接受、拒绝或继续推敲。'],git:['03 / WORKBENCH','Git 源码管理','分支、变更与提交不离开工作台，让每次创作留下清晰的版本航迹。'],wiki:['04 / WORKBENCH','项目索引与 Wiki','从符号、文档和架构三个视角快速理解项目，不必在陌生代码里独自潜行。'],memory:['05 / CORE','本地智能层','本地向量嵌入与长期记忆，让 AI 越用越懂你的项目，数据不出本机。'],security:['06 / CONTROL','权限与安全','敏感操作逐一确认，计划模式先规划后执行，让每次行动都有边界。']};var podDetail=document.getElementById('pod-detail');document.querySelectorAll('[data-pod]').forEach(function(b){b.addEventListener('click',function(){var d=podData[b.dataset.pod];podDetail.innerHTML='<span>'+d[0]+'</span><h3>'+d[1]+'</h3><p>'+d[2]+'</p>';document.querySelectorAll('[data-pod]').forEach(function(x){x.classList.remove('active')});b.classList.add('active')})});document.querySelectorAll('[data-orbit]').forEach(function(b){b.addEventListener('click',function(){document.querySelectorAll('[data-orbit]').forEach(function(x){x.classList.remove('active')});b.classList.add('active');var show=b.dataset.orbit==='extend';document.querySelectorAll('.pod').forEach(function(p){p.style.opacity=show?'0.55':'1'});podDetail.querySelector('span').textContent=show?'EXTEND / ECOSYSTEM':'01 / WORKBENCH'})});
var relay=document.getElementById('relay'),token=document.getElementById('relay-token'),target=document.getElementById('device-b'),instruction=document.getElementById('relay-instruction'),audit=document.getElementById('audit-card'),events=[['同主题验证','发现 Orca / B：同一局域网，同一工作区主题。发现不等于共享。'],['共享范围','只选择需求、原型与任务记录进入这次协作。边界由 A 掌控。'],['多方签名','device:A 与 device:B 为记录签名，来源与时间被一并保存。'],['上下文注入','B 收到的不只是一条消息，还有关联原型、设计与对话上下文。'],['接续执行','任务在 B 设备继续运行，新的结果沿同一条链留下记录。']],eventButtons=[].slice.call(document.querySelectorAll('[data-event]')),relayIndex=0,relayTimer=null,relayPlaying=!reduce;function showRelay(i){relayIndex=i;eventButtons.forEach(function(b,n){b.classList.toggle('active',n===i)});audit.innerHTML='<span>EVENT '+String(i+1).padStart(2,'0')+'</span><h3>'+events[i][0]+'</h3><p>'+events[i][1]+'</p>';token.className='relay-token relay-'+i;instruction.childNodes[0].textContent=i===3?'上下文已注入 Orca / B':'按住任务信号，拖到 Orca / B'}eventButtons.forEach(function(b){b.addEventListener('click',function(){showRelay(Number(b.dataset.event));stopRelay()})});function stopRelay(){relayPlaying=false;clearInterval(relayTimer);relayTimer=null;document.getElementById('relay-auto').textContent='继续自动演示'}function startRelay(){if(reduce)return;relayPlaying=true;document.getElementById('relay-auto').textContent='暂停自动演示';clearInterval(relayTimer);relayTimer=setInterval(function(){showRelay((relayIndex+1)%events.length)},2800)}document.getElementById('relay-auto').addEventListener('click',function(){relayPlaying?stopRelay():startRelay()});document.getElementById('relay-reset').addEventListener('click',function(){showRelay(0);startRelay()});function handoff(){showRelay(4);stopRelay();instruction.childNodes[0].textContent='接力完成：Orca / B 正在继续执行'}token.addEventListener('keydown',function(e){if(e.key==='Enter'||e.key===' '){e.preventDefault();handoff()}});token.addEventListener('pointerdown',function(event){token.setPointerCapture&&token.setPointerCapture(event.pointerId);token.classList.add('dragging');function move(e){var r=relay.getBoundingClientRect();token.style.left=Math.max(3,Math.min(83,(e.clientX-r.left)/r.width*100-8))+'%';token.style.top=Math.max(12,Math.min(70,(e.clientY-r.top)/r.height*100-6))+'%'}function up(e){token.releasePointerCapture&&token.releasePointerCapture(e.pointerId);token.classList.remove('dragging');var tr=target.getBoundingClientRect();if(e.clientX>tr.left-45&&e.clientX<tr.right+45&&e.clientY>tr.top-50&&e.clientY<tr.bottom+50)handoff();else{token.style.left='27%';token.style.top='37%'}}token.addEventListener('pointermove',move);token.addEventListener('pointerup',up,{once:true})});showRelay(0);if(relayPlaying)startRelay();
})();