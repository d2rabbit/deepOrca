# design/ — DeepOrca UI/UX 重设计（Orca Deck v3.1）

> 2026-08-19 · 从 `docs/research/` 迁移收敛而来。这里是重设计的**有效交付物**；过时中间稿已删除。

## 文件

| 文件 | 内容 | 状态 |
|---|---|---|
| `index.html` | **交互 Demo（主交付物）**：v3.1 工单工作台，单文件、零依赖，浏览器直接打开 | ✅ 当前有效 |
| `vision.md` | 设计愿景：为什么脱离聊天本体（对话→记录仪，工单→界面本体，回合制→并行值守） | ✅ |
| `wireframes.md` | 交互规格：线框、尺寸、键盘地图、浮层栈、token 增补 | ✅ |
| `coverage.md` | 模块覆盖矩阵：审计报告每个模块 → v3 落点 → 基座稿帧号 | ✅ |
| `base-complete-design.html` | **基座参考**：27 帧全模块静态设计稿（Liquid Glass 主视觉） | 📌 基座，只读参考 |
| `../2026-08-19-ui-ux-audit-report.md` | 现状审计（在 research/ 根目录，非设计稿故未迁移） | ✅ |

## Demo 交互清单（index.html）

- **范式**：工单即界面——步骤板（点击选中/双击划线/gate 点击切换）、步骤卡（范围 chips 可删、进度流、介入批注、待决内联）
- **控制中心**（⌘⇧O / 右缘拉手，手机控制中心式小浮窗，右缘居中）：顶部快捷格（通知/主题/记录/设置）→ 仪表四宫格（时长/成本/上下文/token）→ 指令输入（地址化选中对象，⌘↵）→ 我的指令（全部用户动作留痕）→ 状态观测（引擎事件流）
- **浮层（全模块，审计全覆盖）**：⌘K 命令层（子序列模糊评分，注册表含全部模块）· ⌘⇧M 车间墙 · ⌘T Tape 记录仪 · ⌘, 设置 · 设计资产库（点卡 → 焦点卡预览）· 代码审查（意见一键转介入）· 知识源仪表盘（重建/召回测试）· Token 账本（按工单归账 + 当前工单实时）· 任务树画布（git-graph，点节点切快照/分叉/回填）· 插件与 MCP（启停）· ⌘⇧Z 检查点恢复 · ⌘? 快捷键表 · 分支冲突三选项（变更抽屉"提交并推送"触发）· ⌘N 新目标工单起草（gate 逐行可改、盖章下发即入车间墙）· 全屏编辑器（只读演示，生产接 Monaco）
- **抽屉**：⌘E 文件（点文件 → 焦点卡 diff）、⌘⇧E 变更、⌘⇧P 进程（实时日志）、⌘⇧N 通知（toast 落档）
- **控制**：自律度三档（⌥1/2/3 或点击旋钮）、⏸ 刹车（Space，冻结/恢复）、⌘. 专注模式
- **引擎模拟**：开工后工单自动施工——进度追加、token/成本/上下文走时、80% 触发压缩观测、高危升起待决、步骤闸门前等待、交付 toast
- **六主题实时切换**（🎨 或 ⌘K 搜主题）：Liquid Glass / Flat / Glassmorphism / Neumorphism / Claymorphism / Vernacular 1997

## 版本演进

v1 五区 rail（被否：VS Code 影子）→ v2 召唤式浮层（被否：只动 chrome）→ v3 工单工作台（换本体）→ **v3.1**：删底部指示条（聊天框残迹），指令/仪表收进右缘控制中心（当前 demo）。

## 注意

- Demo 是设计验证物，非生产代码；正式实现以 `specs/` 为准。
- 已知取舍：快捷键监听用 `document` 派发（demo 环境）；Playwright 截图时需临时禁用 `backdrop-filter`（合成器超时）。
