# 变更清单与按需测试导览（2026-09-04 批次）

> 目的：把**拉取下来的变动**（远端他人工作）与**本会话的变动**（我的提交）分开列出，按可测试的功能域组织——你想测哪块，查表即知：改了什么、默认开还是关、怎么触发、期望什么。
> 测试步骤详情见 [功能测试清单](./functional-test-checklist-2026-09-04.md)（下称"清单"）。
> 提交范围：拉取 = `9cf770bc..677083b9`（12 个远端提交）；本会话 = `f2dddfbe..HEAD`（19 个提交）。

---

# 第一部分：拉取下来的变动（远端 12 提交，他人工作——我只验证过编译与测试，未做功能测试）

## R1. 编辑器多文件工作区重构（6 个提交，最大的一块）

| 项 | 说明 |
| --- | --- |
| 改了什么 | 单文件 `EditorOverlay`（-824 行）退役，换多文件架构：`EditorWorkspace` 组合根（模型按文件路径切换、保活）+ `EditorTabBar` 子 tab 栏（两级脏关闭守卫、保存/编辑回写）+ `use-editor-workspace` 状态钩子 |
| 数字体浮窗 | `EditorAgentFloat`：编辑器内的 agent 浮窗**追随选区**（锚定末行、滚动/选区变化重锚、提交后冻结）+ 数字体在侧栏登记新入口 |
| 诊断抽屉 | `EditorDiagnosticsDrawer`：Monaco 错误底部折叠条（按严重度分组、点击跳转行） |
| 入口 | 侧栏编辑器图标 → 打开文件即进工作区 |
| 怎么测 | 打开 2-3 个文件切换 tab；改一个不保存点关闭（应弹脏守卫）；选中一段文字（数字体浮窗应贴上来）；有语法错误的文件看底部抽屉点击跳转 |
| 主要提交 | `6b8a54ef`（工作区）、`2e4dfeb5`/`2ac42c6a`（tab 栏）、`be2e0431`（浮窗）、`40b83fa2`（抽屉）、`46b9643b`（登记） |

## R2. LSP 诊断桥 P0+P1（2 个提交，需要手动开启才生效）

| 项 | 说明 |
| --- | --- |
| 改了什么 | 类型级诊断能力：真实语言服务器（tsserver/pyright/rust-analyzer 等**十族**）经 stdio MCP 桥接入；回合结束对改动文件双路并列诊断（Serena 语法级 + LSP 类型级），合并去重后注入系统消息 |
| **默认状态** | **关**。设置 `~/.deepcode/settings.json` 加 `"lspDiagnostics": { "enabled": true, "trigger": "auto" }` 才生效（trigger=manual 时只在回合末跑 Serena） |
| 生命周期纪律 | 语言服务器按需拉起 / 空闲 30s 回收 / 回合结束强杀（不常驻） |
| 怎么测 | 开启后让 agent 改一个含类型错误的 .ts → 回合末系统消息应同时含语法与类型错误；活动监视器确认 tsserver 30 秒后消失 |
| 提交 | `27b2ed0d`（P0 桥）、`989af764`（P1 双路+十族） |

## R3. Token 模型热力图（1 个提交）

| 项 | 说明 |
| --- | --- |
| 改了什么 | Token 面板新增"模型热力图"弹窗：7 天 × 24 小时像素图（chips 切模型）+ 速度对比 Top5；账本开始记录每请求 `elapsedMs` |
| 入口 | 左侧 Token 统计面板 → 热力图按钮 |
| 怎么测 | 打开热力图、切换模型 chips、看速度榜有无数据（用得越多越准） |
| 提交 | `244e95eb` |

## R4. 调研文档批次（2 个提交，无代码）

vibegame 预研 / codebrain-membrain 哲学轮 / lsp-idea 可行性 / dsh 封闭 + CMB 台账登记。**不可测试**，但 CMB 台账是本会话全部工作的源头。

---

# 第二部分：本会话的变动（19 个提交，按"默认是否生效"分组——这是按需测试的关键）

## M1. 默认生效（不用任何开关，升级后行为即变）

### M1.1 edit/write 后的诊断提示（CMB-3）

- **变动**：edit/write 成功且目标是代码文件（.ts/.py/.go 等十族扩展名）时，工具结果末尾追加一行"（已修改 xxx.ts，建议立即运行诊断检查该文件。）"；非代码文件（.md/.txt）不追加。
- **测法**：清单 A3/A4。让 agent 改一个 .ts → 看提示；改 .md → 无提示。
- 提交 `95e0ac5d`

### M1.2 诊断诚实化 + 存量缺陷修复（CMB-1/CMB-5，本会话最重要的修复）

- **变动**：① 修复存量缺陷——回合末诊断从上线起就提取不到错误（信封形状不匹配，实证复现后修复）；② 诊断失败不再静默：腿状态入带，"检查通过"与"没跑成"带内可区分，失败注入"⚠️ 部分诊断检查不可用：原因"；③ LSP 桥 spawn 前探测项目依赖（TS 无 node_modules / Python 有 requirements 无 venv / Go 有 require 无 go.sum → 报 deps-missing 并附"run npm install"类补救指引，**不再回灌假阳性 import 错误**）。
- **测法**：清单 B1-B5。核心场景 B3：在没装依赖的 TS 项目让 agent 改代码 → 期望看到降级行而非一堆假错误；装回依赖后立即自愈（探测不缓存）。
- 提交 `007d57e8`

### M1.3 L1 抽取提示词三式 + 记忆渲染时间锚定（CMB-2/CMB-7）

- **变动**：L1 抽取新增三条硬规则（禁从背景对话重复抽取 / 输出前终检时间 / 专名与频率逐字保留）；召回渲染时"上周/昨天"等相对词**原文保留 + 就地标注绝对区间**，无锚标"（源未锚定）"；仅记录时间的记忆显示"（记录于 日期）"而非"活动时间"。
- **测法**：清单 F2/F3。聊天说"我上周做了 X"，之后新会话相关提问 → 召回行里"上周"后应跟"（→ 绝对日期）"。
- 提交 `584440aa`

### M1.4 召回充分性二轮 + 遥测（CMB-8/CMB-10）

- **变动**：首轮召回结果 <2 条时自动用确定性变体再检索一轮（零 LLM）；每次召回输出一行遥测日志。
- **测法**：清单 F4（需看日志）。
- 提交 `3754ea29`

### M1.5 辅助 LLM 调用契约 + 技能匹配模板化（CMB-4）

- **变动**：辅助 LLM 调用（技能匹配/翻译等）增加输出契约校验与内容级重试（≤2 次）；技能匹配提示词从代码字符串改为版本化模板文件。**对外行为应完全不变**——此项是回归性测试（清单 A5 技能匹配正常即可）。
- 提交 `1e5cdf95`

## M2. 默认关闭（需要开关/指令才触发）

### M2.1 复杂性双轨 depth-lane（**开启 `complexityGate.enabled` 后生效**）

```json
// ~/.deepcode/settings.json 顶层加：
"complexityGate": { "enabled": true }          // 轻轨标记+评分（近零成本）
// 可选再加 "depthLaneEnabled": true            // 真正跑重轨 S1-S5（贵，~100s/次）
```

- **开启后可见**：① 会话卡 lane 徽标（青 ⚡轻轨 / 琥珀 ▤重轨）；② 重轨运行时消息区上方**阶段进度条**（S1 情境编译 → … → S5 判定输出，带轮次，完成自隐）；③ 重轨产出 `<proposed_plan>` 形态六段深度报告（结论先行）；④ 设置面板 About 区顶部"车道观察（只读）"——轻/重轨会话数、追问率/负反馈率（历史会话 L1 代理回溯，标 L1×N）。
- **关闭时**：字节级等价（无徽标/无进度条/无额外调用，测试锁定）。
- **测法**：清单 C1-C8（核心：C2 复杂问题进重轨、C3 进度条序列、C5 中断干净、C7 关掉后等价）。
- 提交 `500fdff7`（P0+P1+P2.1/2.2）、`cdf2d162`（率公式+真机校准）、`b40d36d5`/`181530b4`（徽标）、`dc083ff0`（进度流+面板）

### M2.2 记忆审计 memory.audit（对 agent 说自然语言即触发）

- **"运行 memory.audit"**（默认 dryRun 只读）：扫本工作区会话历史失败证据 → 佐证模式（≥2 独立会话）→ 返回快照。
- **"memory.audit 并合成建议"**：产出记忆规则提案（≤5 条，每条带 ev-N 证据）+ HTML 报告落 `.deeporca/audits/`；agent 会用 AskUserQuestion 逐项问 accept/reject/skip；**接受项的写回强制走原生 edit 工具**（弹权限）；任何已决策项永不再现。
- **测法**：清单 D1-D6。
- 提交 `19b3bcf9`（P0）、`3754ea29`（P1+P2）

### M2.3 SOP 萃取 memory.distill（对 agent 说自然语言即触发）

- **"memory.distill 最近会话"**：从**成功**会话蒸馏可复用 SOP——提案可为新建技能（完整 SKILL.md 草案）、AGENTS.md 规则、既有技能增补；审核环与决策库与 memory.audit 共享；新建技能的写回走原生 write 工具。
- **测法**：清单 E1-E5（核心：E4 接受一个新技能 → `.deeporca/skills/<名字>/` 落盘且技能列表出现）。
- 提交 `35790718`

## M3. 纯视觉/国际化（跟随语言设置，无功能开关）

- lane 徽标 / 进度条 / 观察面板的全部文案：**六语言全量**（en/zh/zh-tw/zh-hk/ja/ko）。切语言即可测（清单 G1-G2）。

---

# 第三部分：按需测试速查表

| 我想测… | 前置 | 清单节 | 预计耗时 |
| --- | --- | --- | --- |
| 什么都没配置，升级后有没有坏 | 无 | A1-A5 | 10 分钟 |
| 编辑器多文件/数字体浮窗（拉取的） | 无 | 本文档 R1 | 15 分钟 |
| LSP 类型级诊断（拉取的桥 + 我的诚实化） | 开 `lspDiagnostics` | B2 + R2 | 10 分钟 |
| 诊断不再骗我（假阳性防线） | 找个没装依赖的项目 | B3-B5 | 10 分钟 |
| 轻轨/重轨分流 + 徽标 + 进度条 | 开 `complexityGate` | C1-C5 | 20 分钟 |
| 设置面板车道数据 | 开 `complexityGate`（或直接看历史回溯） | C6-C8 | 3 分钟 |
| 让 agent 自己学规矩（审计） | 无（直接对它说） | D1-D6 | 15 分钟 |
| 让 agent 把成功做法固化成技能 | 无 | E1-E5 | 15 分钟 |
| 记忆召回的时间标注 | 正常聊几轮 | F2-F3 | 5 分钟 |
| 六语言完整性 | 设置切语言 | G1 | 5 分钟 |
| Token 热力图（拉取的） | 无 | R3 | 3 分钟 |

# 附：工作区状态说明

- 未提交文件（`session-trace.test.ts`、`lsp-client.ts`、`server-specs.ts`、`lsp-bridge-seam.test.ts`、`session-skills-mcp.test.ts`、astryx 预研、designs/）属于**并行会话在途工作**，不在任何提交里——从本树打包会带上它们（已全量测试绿），fresh clone 则不含。
- 我提交中携带的三处并行在途块（settings 的 lsp 合并、App 模态抽取、lsp-bridge 测试修复）已在各自提交信息注明——它们是提交树自洽所必需。
